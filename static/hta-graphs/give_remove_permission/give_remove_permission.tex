\documentclass[dvisvgm]{standalone}

% Graphiken
\usepackage{tikz}

% TikZ Bibliotheken
\usetikzlibrary{
    arrows,
    arrows.meta,
    decorations,
    backgrounds,
    positioning,
    fit,
    petri,
    shadows,
    datavisualization.formats.functions,
    calc,
    shapes,
    shapes.multipart
}

\begin{document}
\textcolor{white}{workaround}
\begin{tikzpicture}[sibling distance=3cm, level distance=10cm]

  \node[
    draw,
    text width=2cm,
    minimum height=4cm
  ] at (0,-1)
  {1. give/remove permission}
    % 1.1. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=4cm
      ]
      {1.1. open system's management mobile app}
    }
    % }}}
    % 1.2. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=4cm
      ]
      {1.2. login}
    }
    % }}}
    % 1.3. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=4cm
      ]
      {1.3. select room/trans\-ponder and search for borrower}
    }
    % }}}
    % 1.4. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=4cm
      ]
      {1.4. look at open permission requests (created by borrower in task 5.
        ask for permission)}
    }
    % }}}
    % 1.5. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=4cm
      ]
      {1.5. search for transponder's number, room number or borrower in
        granted permissions}
    }
    % }}}
    % 1.6. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=4cm
      ]
      {1.6. give/remove permission}
        % 1.6.1. {{{
        child {
          node[
            draw,
            text width=2cm,
            minimum height=4cm
          ]
          {1.6.1. make entry specifiing the borrower and a expiring date}
        }
        % }}}
        % 1.6.2. {{{
        child {
          node[
            draw,
            text width=2cm,
            minimum height=4cm
          ]
          {1.6.2. remove entry with borrower's information}
        }
        % }}}
    }
    % }}}
    % 1.7. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=4cm
      ]
      {1.7. confirm update}
    };
    % }}}

\end{tikzpicture}
\end{document}
