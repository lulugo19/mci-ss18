\documentclass[dvisvgm]{standalone}

% Graphiken
\usepackage{tikz}

% TikZ Bibliotheken
\usetikzlibrary{
    arrows,
    arrows.meta,
    decorations,
    backgrounds,
    positioning,
    fit,
    petri,
    shadows,
    datavisualization.formats.functions,
    calc,
    shapes,
    shapes.multipart
}

\begin{document}
\textcolor{white}{workaround}
\begin{tikzpicture}[sibling distance=3cm, level distance=8cm]

  \node[
    draw,
    text width=2cm,
    minimum height=3cm
  ] at (0,-1)
  {1. give/remove permission}
    % 1.1. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {1.1. open system's management-tool in webbrowser}
    }
    % }}}
    % 1.2. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {1.2. login}
    }
    % }}}
    % 1.3. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {1.3. search transponder's number or room number}
    }
    % }}}
    % 1.4. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {1.4. give/remove permission}
        % 1.4.1. {{{
        child {
          node[
            draw,
            text width=2cm,
            minimum height=3cm
          ]
          {1.4.1. make entry specifiing lender's matrikelnumber and name}
        }
        % }}}
        % 1.4.2. {{{
        child {
          node[
            draw,
            text width=2cm,
            minimum height=3cm
          ]
          {1.4.2. remove entry with lender's information}
        }
        % }}}
    }
    % }}}
    % 1.5. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {1.5. confirm update}
    };
    % }}}

\end{tikzpicture}
\end{document}
