\documentclass[dvisvgm]{standalone}

% Graphiken
\usepackage{tikz}

% TikZ Bibliotheken
\usetikzlibrary{
    arrows,
    arrows.meta,
    decorations,
    backgrounds,
    positioning,
    fit,
    petri,
    shadows,
    datavisualization.formats.functions,
    calc,
    shapes,
    shapes.multipart
}

\begin{document}
\textcolor{white}{workaround}
\begin{tikzpicture}[sibling distance=3cm, level distance=8cm]

  \node[
    draw,
    text width=2cm,
    minimum height=3cm
  ] at (0,-1)
  {0. lend transponder}
    % 0.1. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {0.1. visit gate staffs' office}
    }
    % }}}
    % 0.2. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {0.2. ask for tranponder/room number}
    }
    % }}}
    % 0.3. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {0.3. identify yourself}
        % 0.3.1. {{{
        child {
          node[
            draw,
            text width=2cm,
            minimum height=3cm
          ]
          {0.3.1. show Multica}
        }
        % }}}
        % 0.3.2. {{{
        child {
          node[
            draw,
            text width=2cm,
            minimum height=3cm
          ]
          {0.3.2. show ID and tell matrikelnumber}
        };
        % }}}
    }
    % }}}
    % 0.4. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {0.4. sign on pad}
    }
    % }}}
    % 0.5. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {0.5. take transponder}
    }
    % }}}
    % 0.6. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {0.6. go to room and open it}
    }
    % }}}
    % 0.7. {{{
    child {
      node[
        draw,
        text width=2cm,
        minimum height=3cm
      ]
      {0.7. when leaving close room's door and give back transponder}
    };
    % }}}

\end{tikzpicture}
\end{document}
